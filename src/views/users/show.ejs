<!DOCTYPE html>
<html>
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>

    <% include ../static/partials/navbar.ejs %>

    <main class="container">

      <% include ../static/partials/messages.ejs %>


      <h2> Latest Favorited Posts</h2>

      <ul class="list-unstyled">


      <% if(result["posts"]) { %>
        <% if(result["favorites"]) { %>

            <li>
              <a href="/topics/<%= favorite.Post.topicId %>/posts/<%= favorite.Post.id %>">
                <%= favorite.Post.title %>
              </a>
            </li>

            <% } %>
        <% } else { %>
          <p> You have no favorites currently, go read some posts and add to your collection of Favorite Posts! </p>
        <% } %>

      </ul>
      <hr>

      <h2> Latest Comments</h2>

      <ul class="list-unstyled">


        <% if(comments) { %>
          <% comments.forEach((comment) => { %>

            <li>
              <span>Post: </span><a href="/topics/<%= comment.Post.topicId %>/posts/<%= comment.Post.id %>">
                <%= comment.Post.title %>
              </a>
              <p>Comment: <%= comment.body %></p>
            </li>
            <hr>
          <% }) %>
        <% } %>
      </ul>

    </main>

    <% include ../static/partials/baseScripts.ejs %>

  </body>
</html>
